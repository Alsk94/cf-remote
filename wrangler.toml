# Cloudflare Pages Configuration
# This is a PAGES project, NOT a Workers project
# Deploy using: wrangler pages deploy ./out
# Or via Cloudflare Dashboard with Git integration

name = "cf-remote"
pages_build_output_dir = "./out"
